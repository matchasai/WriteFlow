services:
  - type: web
    name: writeflow
    runtime: node
    rootDir: server
    buildCommand: npm ci && cd ../client && npm ci && npm run build
    startCommand: node server.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: SERVE_CLIENT
        value: "true"
