services:
  - type: web
    name: writeflow-client
    runtime: static
    buildCommand: cd client && npm ci && npm run build
    staticPublishPath: client/dist
    routes:
      # SPA support: deep links like /admin should serve index.html
      - type: rewrite
        source: /*
        destination: /index.html
